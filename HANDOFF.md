# State Directed Engineering handoff

## Objective and outcome

`WI-0032` built and wired for publication the **Ordo website** — the public
presentation of this methodology — as an F#-generated, evidence-traceable
static site deployed through GitHub Pages.

The site is authored under `site/`, generated by `src/Ordo.Site`, tested by
`tests/Ordo.Site.Tests`, and deployed by `.github/workflows/pages.yml`. It
publishes 26 pages, 7 experiments, 39 metrics, 20 evidence-map propositions and
a 36-term glossary, with every quantitative claim carrying a definition, a
collection method, a stated limitation and a resolvable source artifact.

The site is evidence-bounded in the same way the method is. Five propositions
are published as **Contradicted** and one as **Open** with the same prominence
as the supported ones, including the programme's most quotable figure (a 68%
token reduction), which is published as *not established* because the run that
produced it was interrupted and its telemetry covers a partial run only.

## Authoritative changes

- `research/decisions/DF-SDE-2026-0005` records the Ordo/SDE naming decision.
  Ordo is the public name; State-Directed Engineering remains the name in the
  research record; **nothing under `research/`, `doctrine/`, `method/`,
  `framework/`, `registries/`, `templates/`, `src/Sde.*` or `distribution/` was
  renamed**, because those names carry the provenance chain every published
  metric depends on.
- `site/README.md` is the maintenance contract for the site: where authority
  lives, how to add a page, how to add evidence, what the build refuses, and
  what must not be changed without new evidence.
- `context/ARCHITECTURE.md` gains a semantic-map row routing to it.
  `AGENTS.md` gains a routing paragraph. Neither restates semantic authority.
- `site/data/evidence.json` is the site's evidence manifest. It is a
  presentation of authority that lives in `research/evidence/`,
  `doctrine/EVIDENCE-TO-ENGINEERING-MAP.md`, `doctrine/GLOSSARY.md` and the
  experiment repositories — not a second authority. Doctrine changes do not
  propagate to it automatically, by design; update both in one change.

## How the site enforces its own claims

The generator makes an unsourced number unrepresentable rather than
discouraged:

- A metric record without a non-empty `limitation`, or without a resolvable
  `source`, cannot be parsed into the model. An experiment without at least one
  `limitations` entry cannot either.
- Prose references metrics by id (`{{metric:ID}}`). An id that does not exist
  fails the build, so a stale or invented figure cannot be published.
- Cross-references are resolved at parse time: a metric naming an unknown
  experiment, or a source naming an unknown repository, fails the build.
- The whole site is assembled in memory and validated before anything is
  written: required pages, internal link resolution, cross-page fragment
  targets, heading structure, landmarks, metadata, draft markers, credential
  shapes, and generated-JSON parseability. On failure nothing is written and
  the previous output is left untouched.

## Verification actually performed

- **Not run: `dotnet build` and `dotnet test`.** The .NET SDK could not be
  installed in the session container — `builds.dotnet.microsoft.com` is denied
  by the egress policy (HTTP 403 on CONNECT). The F# in `src/Ordo.Site` and
  `tests/Ordo.Site.Tests` has therefore **never been compiled locally**. It is
  first compiled and tested by `.github/workflows/pages.yml`. This is a real
  gap, stated rather than glossed.
- **Run: a content pre-flight.** A throwaway script (scratchpad only, not
  committed) reproduced the generator's token resolution and validation rules
  closely enough to check the authored content: 26 pages, 0 errors. It proves
  nothing about the F# itself.
- **Run: `./ros registry check`, `./ros registry build`, `./ros validate`** —
  validation passed.
- **Run: JSON validity of `site/data/evidence.json` and `site/data/site.json`,
  plus cross-reference integrity** (every metric's experiment, every source's
  repository) at generation time.

## Evidence discovery

Five repositories were located and inspected directly, not assumed:

- `kemiller2002/helix-note-application` — controlled experiments 1–3.
- `kemiller2002/SDE-Engineering-Trial-1-Greenfield-Construction` — requirements
  fixture and metrics instrumentation (branch
  `claude/dev-metrics-instrumentation-dfnzhq`).
- `kemiller2002/SDE-Engineering-Trial-2-Greenfield-Construction` — experiment 2
  and its independent replication 2B (`main` is an empty initial commit; the
  work is on two `claude/greenfield-*` branches).
- `kemiller2002/time-entry-state-machine` — the effort experiment.
- `kemiller2002/consulting-company` — the Echelon Foundry design system.

## Evidence gaps and risks

- GitHub Pages is configured with *Source: GitHub Actions* and the custom
  domain `ordo.echelonfoundry.com`. Both were already set on the repository;
  this mission discovered the domain from the deployment record, not before it.
- `site/data/site.json` targets that domain at its root. `customDomain`,
  `baseUrl` and `pathPrefix` now carry a mechanical agreement check, so the
  mismatch that broke the first deployment cannot be expressed again.
- No Limen artifact exists in any of the six repositories inspected. The site
  makes no claim about it and says so explicitly on `/about/`.
- The evidence base remains one organisation, four languages, single-agent
  execution, and agents from one model family. The site states all four limits
  on `/evidence/` and `/research/`.
- The site duplicates doctrine in prose. If doctrine changes and
  `site/data/evidence.json` does not, they drift silently. `site/README.md`
  names this and requires both to change together.

## Correction after the first deployment

PR #8 merged and deployed successfully, and the deployed site was wrong.

GitHub Pages on this repository is configured with the custom domain
`ordo.echelonfoundry.com`, which serves from the domain root. `site.json` was
written for a project-page path (`/state-directed-engineering`), so every
internal link, the stylesheet, the favicon and every canonical URL pointed at a
path that does not exist on that domain. The page loaded unstyled with no
working navigation.

Nothing caught it. Every check passed, because each field was internally valid
and no mechanism compared `baseUrl`, `pathPrefix` and the actual deployment
target. It is the same failure class this site's own evidence pages describe:
one fact maintained in several places with no agreement check between them —
found, as in every trial in that evidence base, only by looking at the real
boundary rather than by any mechanical check.

The fix adds `customDomain` to the site config and a mechanical agreement
check: with a custom domain set, `baseUrl` must be `https://<customDomain>` and
`pathPrefix` must be empty, or the build fails and reports both halves.
`tests/Ordo.Site.Tests/SiteConfigTests.fs` pins the exact shape of the original
defect. The build now also writes a `CNAME` into the artifact.

## Next recommended action

1. Merge, then set **Settings → Pages → Source: GitHub Actions**, and confirm
   the first deployment.
2. Fix whatever the first CI compile reports. Expect the F# to need at least
   one correction, given it has never been built.
3. Then resume `WI-0019`: scope and execute the first controlled validation on
   a non-HelixNote repository per `method/FIRST-VALIDATION-DESIGN.md`. The
   greenfield trials are construction records, not the controlled validation
   that design calls for.
